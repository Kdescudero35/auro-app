diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index 9f0c48a..56d2c5e 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -124,7 +124,9 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
                         const state = getStateFromURL(url);
                         if (typeof url === 'string') {
                             // If the link were handled, it gets cleared in NavigationContainer
-                            onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                            setTimeout(() => {
+                                onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                            }, 0);
                         }
                         return state;
                     });
diff --git a/node_modules/expo-router/build/react-navigation/native/useLinking.native.js b/node_modules/expo-router/build/react-navigation/native/useLinking.native.js
index f74ecd7..2418415 100644
--- a/node_modules/expo-router/build/react-navigation/native/useLinking.native.js
+++ b/node_modules/expo-router/build/react-navigation/native/useLinking.native.js
@@ -129,7 +129,9 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
                         const state = getStateFromURL(url);
                         if (typeof url === 'string') {
                             // If the link were handled, it gets cleared in NavigationContainer
-                            onUnhandledLinking((0, extractPathFromURL_1.extractPathFromURL)(prefixes, url));
+                            setTimeout(() => {
+                                onUnhandledLinking((0, extractPathFromURL_1.extractPathFromURL)(prefixes, url));
+                            }, 0);
                         }
                         return state;
                     });
